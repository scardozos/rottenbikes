apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ui-route
  namespace: rottenbikes
spec:
  parentRefs:
  - name: rottenbikes-dev
    sectionName: ui-https
  hostnames:
  - dev.rottenbik.es
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: ui-dev
      port: 8080
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: api-route
  namespace: rottenbikes
spec:
  parentRefs:
  - name: rottenbikes-dev
    sectionName: api-https
  hostnames:
  - api-dev.rottenbik.es
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: api-dev
      port: 8080
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ui-redirect
  namespace: rottenbikes
spec:
  parentRefs:
  - name: rottenbikes-dev
    sectionName: ui-http
  hostnames:
  - dev.rottenbik.es
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
        port: 443
